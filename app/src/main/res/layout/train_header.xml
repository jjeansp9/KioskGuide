<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/train_header_height"
    android:background="@color/train_bg_color1"
    >

    <RelativeLayout
        android:id="@+id/header_backbtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="12dp"
        >

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/btn_back_train"
            />

    </RelativeLayout>

    <TextView
        android:id="@+id/header_title"
        style="@style/AutoSize"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:maxLines="1"
        android:text="@string/train_main_title1"
        android:textColor="@color/white"
        android:textSize="@dimen/train_font_main_title" />

    <android.widget.Button
        android:id="@+id/btn_back_main"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/header_home_btn_size"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="12dp"
        android:background="@drawable/btn_main_selector"
        android:text="@string/back_main"
        android:textColor="@color/white"
        android:textSize="@dimen/layout_margin"
        android:textStyle="bold" />

</RelativeLayout>